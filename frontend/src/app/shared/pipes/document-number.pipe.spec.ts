import { DocumentNumberPipe } from './document-number.pipe';

describe('DocumentNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new DocumentNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
